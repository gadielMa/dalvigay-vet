"use client";
import { FormEvent, useState } from "react";
import { useRouter } from "next/navigation";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
export function NuevaMascota({ clienteId }: { clienteId: number }) { const router=useRouter(); const [error,setError]=useState(""); const submit=async(e:FormEvent<HTMLFormElement>)=>{e.preventDefault();setError("");const data=Object.fromEntries(new FormData(e.currentTarget));const r=await fetch("/api/pacientes",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({...data,pac_cliente:clienteId})});if(!r.ok)return setError((await r.json()).error??"No se pudo crear");e.currentTarget.reset();router.refresh()}; return <details className="rounded-lg border bg-white p-2"><summary className="cursor-pointer text-sm font-medium text-slate-700">+ Nueva mascota</summary><form onSubmit={submit} className="mt-3 grid gap-2 sm:grid-cols-2"><Input name="pac_nombre" placeholder="Nombre" required/><Input name="pac_apellido" placeholder="Apellido (opcional)"/><Input name="pac_fecha_nac" type="date"/><select name="pac_raz_siglas" className="rounded-lg border px-2 text-sm"><option value="C">Canino</option><option value="F">Felino</option><option value="AVE">Ave</option><option value="OTRO">Otro</option></select><Input name="pac_raz_nombre" placeholder="Raza"/><select name="pac_sexo" className="rounded-lg border px-2 text-sm"><option value="">Sexo</option><option>Macho</option><option>Hembra</option></select><Input name="pac_color" placeholder="Color"/><Input name="pac_peso" placeholder="Peso (kg)"/><Input name="pac_microchip" placeholder="Microchip"/><textarea name="pac_obser" placeholder="Observaciones" className="min-h-20 rounded-lg border p-2 text-sm sm:col-span-2"/><Button type="submit">Crear mascota</Button></form>{error&&<p className="mt-2 text-sm text-red-600">{error}</p>}</details> }
