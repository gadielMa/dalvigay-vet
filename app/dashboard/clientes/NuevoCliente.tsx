"use client";
import { FormEvent,useState } from "react";
import { useRouter } from "next/navigation";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
export function NuevoCliente(){const router=useRouter();const[open,setOpen]=useState(false);const[error,setError]=useState("");const submit=async(e:FormEvent<HTMLFormElement>)=>{e.preventDefault();setError("");const r=await fetch("/api/clientes",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(Object.fromEntries(new FormData(e.currentTarget)))});if(!r.ok)return setError((await r.json()).error??"No se pudo crear");const c=await r.json();router.push(`/dashboard/clientes/${c.cli_id}`);router.refresh()};return <details open={open} onToggle={e=>setOpen((e.currentTarget as HTMLDetailsElement).open)} className="mb-4 rounded-xl border bg-white p-3"><summary className="cursor-pointer text-sm font-medium text-slate-700">+ Nuevo cliente</summary><form onSubmit={submit} className="mt-3 grid gap-2 sm:grid-cols-2 lg:grid-cols-3"><Input name="cli_nombre" placeholder="Nombre"/><Input name="cli_apellido" placeholder="Apellido"/><Input name="cli_celu" placeholder="Celular / WhatsApp"/><Input name="cli_tel1" placeholder="Teléfono"/><Input name="cli_mail" type="email" placeholder="Email"/><Input name="cli_domic" placeholder="Domicilio"/><Input name="cli_loc" placeholder="Localidad"/><Input name="cli_prov" placeholder="Provincia"/><Input name="cli_cuit" placeholder="CUIT"/><Button type="submit">Crear cliente</Button></form>{error&&<p className="mt-2 text-sm text-red-600">{error}</p>}</details>}
