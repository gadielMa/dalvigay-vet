"use client";
import { FormEvent, useState } from "react";
import { useRouter } from "next/navigation";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
export function ClinicalAdditions({ pacienteId }: { pacienteId:number }) { const router=useRouter(); const [error,setError]=useState(""); const send=async(e:FormEvent<HTMLFormElement>,url:string)=>{e.preventDefault();setError("");const body=Object.fromEntries(new FormData(e.currentTarget));const r=await fetch(url,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({...body,paciente_id:pacienteId})});if(!r.ok)return setError((await r.json()).error??"No se pudo guardar");e.currentTarget.reset();router.refresh()};return <div className="mb-5 flex flex-wrap gap-2"><details className="rounded-lg border bg-white p-2"><summary className="cursor-pointer text-sm font-medium text-slate-700">🪱 Cargar ecto / endo</summary><form onSubmit={e=>send(e,"/api/ectoendo")} className="mt-3 grid gap-2 sm:grid-cols-2"><Input name="fecha" type="date" required/><Input name="proxima" type="date"/><Input name="tipo" placeholder="Producto / tratamiento" required/><Input name="dr" placeholder="Veterinario/a"/><textarea name="detalle" placeholder="Detalle e indicaciones" className="min-h-20 rounded-lg border p-2 text-sm sm:col-span-2"/><Button type="submit">Guardar tratamiento</Button></form></details><details className="rounded-lg border bg-white p-2"><summary className="cursor-pointer text-sm font-medium text-slate-700">❤️ Cargar electrocardiograma</summary><form onSubmit={e=>send(e,"/api/electrocardio")} className="mt-3 grid gap-2 sm:grid-cols-2"><Input name="fecha" type="date" required/><Input name="dr" placeholder="Veterinario/a"/><Input name="estudio" placeholder="Nombre del estudio" required/><Input name="foto" placeholder="Referencia de foto (opcional)"/><textarea name="diagnostico" placeholder="Diagnóstico" className="min-h-24 rounded-lg border p-2 text-sm sm:col-span-2"/><Button type="submit">Guardar electrocardiograma</Button></form></details>{error&&<p className="w-full text-sm text-red-600">{error}</p>}</div>}
