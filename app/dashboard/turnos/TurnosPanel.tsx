"use client";
import { FormEvent, useMemo, useState } from "react";
import { useRouter } from "next/navigation";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
type Cliente={cli_id:number;cli_nombre?:string;cli_apellido?:string};type Paciente={pac_id:number;pac_nombre?:string;pac_cliente?:number};type Turno={id:number;fecha:string;hora:string;cliente_id:number;paciente_id:number;motivo:string;estado:string;notas?:string|null};
const iso=(date:Date)=>date.toISOString().slice(0,10);const clean=(v:unknown)=>String(v??"").trim();
function monday(value:string){const d=new Date(`${value}T12:00:00`);const day=(d.getDay()+6)%7;d.setDate(d.getDate()-day);return iso(d)}function days(start:string){return Array.from({length:7},(_,i)=>{const d=new Date(`${start}T12:00:00`);d.setDate(d.getDate()+i);return iso(d)})}function shift(start:string,n:number){const d=new Date(`${start}T12:00:00`);d.setDate(d.getDate()+n);return iso(d)}function label(date:string){return new Intl.DateTimeFormat("es-AR",{weekday:"short",day:"numeric",month:"short"}).format(new Date(`${date}T12:00:00`))}
export function TurnosPanel({turnos,clientes,pacientes,today}:{turnos:Turno[];clientes:Cliente[];pacientes:Paciente[];today:string}){const router=useRouter();const[error,setError]=useState("");const[week,setWeek]=useState(monday(today));const[clienteId,setClienteId]=useState("");const weekDays=useMemo(()=>days(week),[week]);const pets=clienteId?pacientes.filter(p=>Number(p.pac_cliente)===Number(clienteId)):pacientes;const owner=(id:number)=>{const c=clientes.find(x=>x.cli_id===id);return `${clean(c?.cli_apellido)} ${clean(c?.cli_nombre)}`.trim()||`Cliente #${id}`};const pet=(id:number)=>clean(pacientes.find(x=>x.pac_id===id)?.pac_nombre)||`Mascota #${id}`;const submit=async(e:FormEvent<HTMLFormElement>)=>{e.preventDefault();setError("");const body=Object.fromEntries(new FormData(e.currentTarget));const r=await fetch("/api/turnos",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(body)});if(!r.ok)return setError((await r.json()).error??"No se pudo guardar");e.currentTarget.reset();setClienteId("");router.refresh()};const change=async(id:number,estado:string)=>{const r=await fetch(`/api/turnos/${id}`,{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({estado})});if(!r.ok)setError((await r.json()).error??"No se pudo actualizar");router.refresh()};return <div className="space-y-5"><div className="flex flex-wrap items-end justify-between gap-3"><div><h1 className="text-xl font-semibold text-slate-800">Turnos</h1><p className="text-sm text-slate-500">Agenda semanal compartida.</p></div><div className="flex items-center gap-2"><Button variant="outline" size="sm" onClick={()=>setWeek(shift(week,-7))}>← Semana</Button><Input type="date" value={week} onChange={e=>setWeek(monday(e.target.value))} className="w-auto"/><Button variant="outline" size="sm" onClick={()=>setWeek(monday(today))}>Hoy</Button><Button variant="outline" size="sm" onClick={()=>setWeek(shift(week,7))}>Semana →</Button></div></div><form onSubmit={submit} className="grid min-w-0 gap-2 rounded-xl border bg-white p-4 sm:grid-cols-2 lg:grid-cols-4"><Input name="fecha" type="date" defaultValue={today} required/><Input name="hora" type="time" required/><select name="cliente_id" value={clienteId} onChange={e=>setClienteId(e.target.value)} required className="h-9 min-w-0 rounded-lg border px-2 text-sm"><option value="">Dueño</option>{clientes.map(c=><option key={c.cli_id} value={c.cli_id}>{clean(c.cli_apellido)} {clean(c.cli_nombre)}</option>)}</select><select name="paciente_id" required className="h-9 min-w-0 rounded-lg border px-2 text-sm"><option value="">Mascota</option>{pets.map(p=><option key={p.pac_id} value={p.pac_id}>{clean(p.pac_nombre)}</option>)}</select><Input name="motivo" placeholder="Motivo" required/><Input name="notas" placeholder="Notas (opcional)"/><Button type="submit">Agregar turno</Button>{error&&<p className="text-sm text-red-600 sm:col-span-2 lg:col-span-4">{error}</p>}</form><div className="grid min-w-[760px] grid-cols-7 gap-2 overflow-x-auto rounded-xl border bg-slate-100 p-2">{weekDays.map(day=>{const dayTurns=turnos.filter(t=>t.fecha===day);return <div key={day} className="min-h-72 rounded-lg bg-white p-2"><div className={`mb-2 border-b pb-2 text-center text-xs font-semibold ${day===today?"text-blue-700":"text-slate-600"}`}>{label(day)}</div><div className="space-y-2">{dayTurns.map(t=><div key={t.id} className={`rounded-md border p-2 text-xs ${t.estado==="cancelado"?"bg-slate-50 opacity-60":t.estado==="atendido"?"border-green-200 bg-green-50":"bg-white"}`}><div className="font-semibold text-slate-800">{String(t.hora).slice(0,5)} · {pet(t.paciente_id)}</div><div className="mt-1 text-slate-600">{owner(t.cliente_id)}</div><div className="mt-1 text-slate-500">{t.motivo}</div><select value={t.estado} onChange={e=>change(t.id,e.target.value)} className="mt-2 w-full rounded border bg-white px-1 py-1 text-[11px]"><option>pendiente</option><option>confirmado</option><option>atendido</option><option>cancelado</option></select></div>)}{!dayTurns.length&&<p className="py-4 text-center text-xs text-slate-300">Sin turnos</p>}</div></div>})}</div></div>}
